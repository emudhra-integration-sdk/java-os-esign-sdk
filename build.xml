<?xml version="1.0" encoding="UTF-8"?>
<project name="eSignASPLibrary" default="jar" basedir=".">

    <property name="src.dir"     value="src"/>
    <property name="build.dir"   value="build"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
    <property name="dist.dir"    value="dist"/>
    <property name="lib.dir"     value="lib"/>
    <property name="jar.name"    value="eSignASPLibrary5_12.jar"/>

    <!-- Classpath: all JARs in lib/ -->
    <path id="compile.classpath">
        <fileset dir="${lib.dir}" includes="*.jar"/>
    </path>

    <target name="clean" description="Remove build and dist directories">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>

    <target name="compile" description="Compile all source files">
        <mkdir dir="${classes.dir}"/>
        <javac srcdir="${src.dir}"
               destdir="${classes.dir}"
               source="11"
               target="11"
               encoding="UTF-8"
               includeantruntime="false"
               excludes="Test.java,Test1.java,TestHarness.java">
            <classpath refid="compile.classpath"/>
        </javac>
        <!-- Copy non-Java resources (PDFBox glyph lists, ICC profiles, CMap tables, etc.) -->
        <copy todir="${classes.dir}">
            <fileset dir="${src.dir}" excludes="**/*.java"/>
        </copy>
    </target>

    <target name="jar" depends="compile" description="Build the SDK JAR">
        <mkdir dir="${dist.dir}"/>
        <jar destfile="${dist.dir}/${jar.name}" basedir="${classes.dir}">
            <manifest>
                <attribute name="Implementation-Title"   value="eSign ASP Library"/>
                <attribute name="Implementation-Version" value="5.12"/>
                <attribute name="Built-By"               value="eMudhra Limited"/>
            </manifest>
        </jar>
        <echo message="Built: ${dist.dir}/${jar.name}"/>
    </target>

    <target name="all" depends="clean,jar" description="Clean and build"/>

</project>
